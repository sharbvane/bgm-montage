interface:
  display_name: "BGM Montage"
  short_description: "Plan, source, render, and QA BGM-led Pixabay montages"
  default_prompt: "Use $bgm-montage v1.2 to learn the read-only reference style, build an audiomap and music-event timeline, reuse or search Pixabay footage, enforce diversity and low-face policies, render without overwriting prior runs, and deliver only after automatic QA passes. If CLIP is unavailable, use structural fallback only when I explicitly allow it."

policy:
  allow_implicit_invocation: true
