interface:
  display_name: "BGM Montage"
  short_description: "Plan, source, render, and QA quality-first BGM montages"
  default_prompt: "Use $bgm-montage v1.4.1 to plan, source, render, and run programmatic QA; then actually view every requested evidence frame, write the structured Agent Visual Review, resume bounded automatic rework when needed, and deliver only after both QA layers pass."

policy:
  allow_implicit_invocation: true
