# Copy this file to the project root as .env. Never commit or package the real .env.
# The scripts read this value without printing it or persisting authenticated URLs.
PIXABAY_API_KEY=your_pixabay_api_key_here
